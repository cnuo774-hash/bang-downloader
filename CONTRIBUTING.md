# Contributing

感谢你改进 Bang Downloader。

1. Fork 仓库并从 `main` 创建分支。
2. 保持改动聚焦，并为行为变化添加测试。
3. 运行 `python3 -m unittest discover -s tests -v`。
4. 提交 pull request，说明问题、实现方式和验证结果。

请不要在 issue、测试或提交记录中包含真实的私有磁力链接、种子内容、认证信息或本地隐私路径。测试应使用虚构数据，且不得依赖真实网络下载。
